// This file contains default environment configuration
// DO NOT commit sensitive credentials to version control
// Create environment.local.ts for local development overrides

export const environment = {
  production: false,
  // Default values - override in environment.local.ts for local development
  defaultEmail: '',
  defaultPassword: '',
};
